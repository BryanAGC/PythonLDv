"""
Dado dos vectores, calcula su producto escalar.
"""
import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
producto_escalar = np.dot(a, b)

print("Producto escalar:", producto_escalar)
